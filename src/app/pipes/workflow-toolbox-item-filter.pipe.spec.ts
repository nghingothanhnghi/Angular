import { WorkflowToolboxItemFilterPipe } from './workflow-toolbox-item-filter.pipe';

describe('WorkflowToolboxItemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkflowToolboxItemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
