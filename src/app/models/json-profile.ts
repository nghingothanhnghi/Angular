import { ComponentModel } from './component'
import { CreationComponentInfo } from './creation-component-info';

export class JsonProfile extends ComponentModel {

    constructor(info: CreationComponentInfo) {
        super(info);
    }
}
